"use client";

import { FormEvent, useState } from "react";
import { AlertTriangle, CheckCircle2, PackageCheck, ShoppingBag, Users } from "lucide-react";
import { useDecision } from "@/components/decision/decision-provider";
import { DataBoundary, EmptyState, WorkspaceHeading } from "@/components/decision/workspace-ui";
import { ResultType } from "@/domain/decision-types";

const resultMeta={human:{label:"真人反馈",icon:Users},sample:{label:"样品表现",icon:PackageCheck},sales:{label:"销售结果",icon:ShoppingBag}} as const;
export default function ResultsPage(){const {state,addResult}=useDecision();const [draft,setDraft]=useState({type:"human" as ResultType,summary:"",outcome:"supports" as "supports"|"conflicts"|"unclear",source:""});const submit=(e:FormEvent)=>{e.preventDefault();addResult(draft);setDraft({...draft,summary:"",source:""})};return <div className="page-frame"><WorkspaceHeading eyebrow="Reality feedback" title="结果回流与校准" description="数字判断、真人反馈、样品表现和销售结果分别记录。只有真实结果才能用于评估偏差并提出规则修正。"/><DataBoundary/><div className="results-layout"><form onSubmit={submit} className="panel-surface" data-guide="results"><p className="section-kicker">Record reality</p><h2 className="mt-2 text-xl font-semibold">回填一项真实结果</h2><div className="mt-5 segmented">{(["human","sample","sales"] as ResultType[]).map(type=><button type="button" key={type} onClick={()=>setDraft({...draft,type})} className={draft.type===type?"active":""}>{resultMeta[type].label}</button>)}</div><label className="form-field mt-5"><span>结果来源</span><input required value={draft.source} onChange={e=>setDraft({...draft,source:e.target.value})} placeholder="访谈编号、样品批次或销售报表"/></label><label className="form-field mt-4"><span>结果摘要</span><textarea required value={draft.summary} onChange={e=>setDraft({...draft,summary:e.target.value})} placeholder="只记录观察到的事实，不补造原因"/></label><label className="form-field mt-4"><span>与原判断关系</span><select value={draft.outcome} onChange={e=>setDraft({...draft,outcome:e.target.value as typeof draft.outcome})}><option value="supports">支持原判断</option><option value="conflicts">与原判断冲突</option><option value="unclear">仍无法判断</option></select></label><button className="primary-action mt-5">保存结果并进入校准账本</button></form><section><div className="result-separation">{(["human","sample","sales"] as ResultType[]).map(type=>{const Icon=resultMeta[type].icon;const count=state.results.filter(x=>x.type===type).length;return <div key={type}><Icon className="h-5 w-5"/><span>{resultMeta[type].label}</span><strong>{count}</strong><small>独立记录</small></div>})}</div>{state.results.length===0?<EmptyState title="尚无真实结果回填" body="当前系统判断仍是待验证假设，不能被包装成已验证成果。"/>:<div className="mt-4 space-y-3">{state.results.map(item=>{const Icon=resultMeta[item.type].icon;return <article key={item.id} className={`result-record ${item.outcome}`}><Icon className="h-5 w-5"/><div><div className="flex flex-wrap gap-2"><strong>{resultMeta[item.type].label}</strong><span className="meta-chip">{item.id}</span></div><p>{item.summary}</p><small>{item.source} · {item.recordedAt}</small><div className="mt-3 flex items-start gap-2 text-xs">{item.outcome==="conflicts"?<AlertTriangle className="h-4 w-4"/>:<CheckCircle2 className="h-4 w-4"/>}{item.ruleChange}</div></div></article>})}</div>}</section></div></div>}
